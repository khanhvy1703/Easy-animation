package cs3500.animator.controller;

/**
 * Interface for the Controller. It takes model and its function and return to the view which shows
 * coordinately.
 */
public interface IController {

  /**
   * Method that start showing animation(visual, textual) through controller.
   */
  void goAction();
}
